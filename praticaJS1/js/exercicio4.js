function verificar(num) {
    let numero = parseInt(prompt("Informe o Número"));
    let intervalo = [30,50];
    let intervalo2 = [60,100];

    if (num>=intervalo[30]) and (num<=intervalo[50]) {
       alert("VC Ganhou");
    }

}